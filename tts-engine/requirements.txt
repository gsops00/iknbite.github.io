# Core Server
flask>=3.0
flask-cors>=4.0
gunicorn>=21.2
numpy>=1.24
soundfile>=0.12

# DSP Pipeline
scipy>=1.11
pyloudnorm>=0.1.0

# Audio Evaluation
# (uses scipy and numpy above — no extra deps)

# TTS Backends (install selectively)
# Kokoro:    pip install kokoro
# Piper:     pip install piper-tts
# MeloTTS:   pip install git+https://github.com/myshell-ai/MeloTTS.git
# Chatterbox: pip install chatterbox-tts

# GPU Acceleration (optional)
# torch>=2.0  # Install with CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121

# Utilities
requests>=2.31
click>=8.1
rich>=13.0
